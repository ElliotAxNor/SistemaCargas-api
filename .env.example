# Django Settings
SECRET_KEY=tu-clave-secreta-aqui-generala-con-comando-abajo
DJANGO_SETTINGS_MODULE=config.settings.production

# Render.com proporcionará automáticamente:
# RENDER_EXTERNAL_HOSTNAME=tu-app.onrender.com

# Hosts permitidos (separados por coma, sin espacios)
ALLOWED_HOSTS=tu-app.onrender.com,localhost

# CORS - URL de tu frontend en Vercel (sin espacios, separados por coma)
CORS_ALLOWED_ORIGINS=https://tu-app.vercel.app,http://localhost:3000

# Security
SECURE_SSL_REDIRECT=True

# Para generar un SECRET_KEY seguro, ejecuta:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
